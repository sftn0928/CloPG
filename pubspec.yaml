name: flutter_clopg
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  firebase_auth: ^3.1.1
  firebase_core: ^1.6.0
  cloud_firestore: ^2.4.0
  cupertino_icons: ^1.0.3
  english_words: ^4.0.0
  adobe_xd: ^2.0.0+1
  flutter_svg: ^0.21.0+1
  flutter_launcher_icons: "^0.9.0"
  image_picker: ^0.8.1+3
  firebase_storage: ^10.0.6
#  cashed_network_image: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - images/sample.jpg
